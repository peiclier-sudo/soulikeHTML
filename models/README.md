# Character Model Folder

Put your character model at:

- `models/hero.glb`

The game is already wired to auto-load this file using `GLTFLoader`.
If loading fails, it will fallback to the procedural hero so gameplay still works.
